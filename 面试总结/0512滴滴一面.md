1. 设计模式用到了哪些
2. java反射，反射底层怎么实现
3. OK。线程池之前用的多吗？线程池。线程池。我在项目里面也用到了这个线程池，就是，对，那我现在这么两个场景啊？嗯，第一个场景就是我希望我用到的是线程池，它里面创建的线程，这些线程的名称是我自定义的。那我有这么个诉求，那你可以如何实现？
4. 我现在的场景是这样的，就是当前我使用的这个拒绝策略默认是直接丢弃掉的，就是比如说我先吃的阻塞队列满了，然后它的线程也到最大数量了，那此时我新进来的任务会触发这个拒绝策略，然后由于我当前拒绝策略是默认丢弃的，那也就意味着我新进来的任务会被直接丢弃掉。ok， 现在当前是这样的，那假如说我现在的诉求是希望你去改一改这个拒绝策略，通过一些自定义的拒绝策略，使这些后面原本要被丢弃掉的任务不被丢弃掉，并且保证我这些任务最终都能被执行到。那对于这样一个诉求，你会如何去自定义你的拒绝策略才来达到这个效果？
5. 那我能在这个拒绝策略里面去借助一些中间件，把这些原本要丢弃的消息或者原本要丢弃的任务先给它存起来，不管是你丢到 MQ 里面，还是丢到这个 Redis 里面，或者说给它直接存到 DB 里面，都，都有可能是个不错的选择。那最后再从中间件里面把这些捞出来再去执行，其实就是找个地方存起来，或者说类似于这样方式去解决这个这个这个卡点吧。然后接下来我们再聊聊这个 spring 啊，就是不管你用 spring 还是用 spring boot 啊，假如说我现在现在有这么个场景，就我写了一段这个，我写了一堆需求吧，然后最终导致我这个项目里面有非常多的这个接口或者说方法，那我现在诉求就是想在这些接口或者方法里面打印它的出餐和入餐，或者说打印它的入餐和出餐，但是我又不想在每个方法里面手动的去写这么两行日志，就比如说手动的打印出餐和入餐，我想一步到位。有没有类似于这种比较快捷方法，能把所有这些我要打印的出入餐的这些接口或方法能一次性的都给它搞好，而不是通过手动在面写这种日志打印的代码来实现。
	可以用 Spring AOP 来实现。首先定义一个切面类，使用 @Aspect 注解标记，然后通过 @Around 注解定义环绕通知，
6. OK， 那这个其实你需要去配置切点嘛？对吧？对对对，那你这，你配置切点里面配的就，就你配切点的时候里面配的是个路径还是一个怎么样的东西？它是。
		回答是路径表达式，
		那如果不是在一个路径下的几个接口呢
	一条update语句的执行过程
7. 就是我们一般经常会遇到一些慢查询的告警嘛，对吧？那我们经常会去说这个假如说你遇到了慢查，你会怎么去分析排查，对吧？那我们经常会谈到一个 explain 这么一个关键字啊？那当我们用 explain 去分析一条具体 SQL 语句的时候，我们一般主要看它的哪几个这个字段啊？
8. OK， 那假如说你现在分析以后，你发现它这个慢查询那条 SQL 啊，它走索引了，但是它走的索引不是你预期的那个索引，你希望手动的指定它走某一条你预期的索引，也就是说你需要手动指定它的这个索引，那这种时候你可以怎么实现？
9. 这两个，我这两个项目主要是这两个项目里面都涉及到了。嗯， OK。我们在聊 MQ 的时候经常会聊到它的有两种模式啊，就消费者、生产者之间有两种模式，一种是推的模式，一种是拉的模式啊。能简单说一下这个推拉模式之间的差异吗？以及我们在什么场景下适合用推的模式？什么场景下适合用拉的模式？
10. 那推拉模式除了这两种场景，就是你刚说两种场景是，是， OK 的，那我们再，嗯，除了这两种场景之外，它推拉模式在这个技术选型上有什么差别？就比如说推的模式对生产者来说它会带来哪些影响？对消费者来说会带来哪些影响？那拉的模式也是一样的。
11. 抽象类和接口的差别吗
12. 假如说这回到你刚才说的那一段，我最后一段都处理完了，我再返回 ACK 的时候网络挂了。那这种时候就你这边会怎么处理呢？
	 我回答的是加一个中间状态--处理中，并且使用定时任务轮询
13. 为什么使用双层token
14. 项目里回过头看有没有什么欠缺的点
	