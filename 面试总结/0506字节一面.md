

skills
prompt,user prompt 和system prompt的区别
我回答了优先级，但是又提出了system prompt如果设置回答为英文，但是user要求中文，会以什么语言输出

Threadlocal讲一下
我直接说了由threadLocal map来实现，thread实例作为对象，要存的值为val
追问：怎么实现隔离的，或者说源码是什么
我说，threadlocal放在线程特有的工作内存中，其他线程访问不到
追问：存储结构是什么，和哈希表有什么不同，
没回答上来
cache aside缓存
答：先更新数据库再删除缓存
在高并发的场景下，是否存在1ms的延迟，导致读取到旧的值，以及如何解决这样的延迟
答：只想到了延迟双删
商户热点信息是否可以只存缓存，
我说不可以，redis在内存中可能宕机，而且要保证缓存和数据库的一致性
追问redis宕机指的是单例模式吗
答是
追问，那可以用消息队列来异步处理这个缓存问题吗，比如访问商户，

skills是什么，我回答使用说明
prompt,user prompt 和system prompt的区别
我回答了优先级，但是又提出了system prompt如果设置回答为英文，但是user要求中文，会以什么语言输出
mcp是什么，我回答接口上下文协议，连接外部工具的接口的统一协议
react是什么，不知道
transformer的kvcache是什么
attention是什么

